const express = require("express");
const shortid = require("shortid");
const URL = require('../models/url')
const {shortUrlGenreate}=require('../controller/url')

const router = express.Router()


 


 module.exports=router